name: nodrix
version: 0.2.0+2
publish_to: 'none'
description: Nodrix Flutter app.

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  url_launcher: ^6.3.1

flutter:
  uses-material-design: true
