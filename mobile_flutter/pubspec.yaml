name: nodrix
version: 1.0.6+7
publish_to: 'none'
description: Nodrix Flutter app.

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  url_launcher: ^6.3.1

flutter:
  uses-material-design: true
